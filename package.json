{
  "name": "action-mustache",
  "version": "1.0.1",
  "description": "",
  "main": "main.js",
  "scripts": {
    "setup": "npm install --userconfig ./.npmrc",
    "test": "jest --clearCache && jest --no-cache --runInBand"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mustache": "^4.2.0"
  },
  "devDependencies": {
    "@actions/core": "^1.10.1",
    "jest": "^29.7.0"
  }
}
