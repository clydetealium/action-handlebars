{
  "name": "action-mustache",
  "version": "2.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "setup": "npm install --userconfig ./.npmrc",
    "test": "jest --clearCache && jest --no-cache --runInBand"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "handlebars": "^4.7.8"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
