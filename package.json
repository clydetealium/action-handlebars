{
  "name": "action-handlebars",
  "version": "2.0.2",
  "description": "",
  "main": "main.js",
  "scripts": {
    "setup": "npm install --userconfig ./.npmrc",
    "test": "jest --clearCache && jest --no-cache --runInBand",
    "build": "ncc build main.js -o dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "handlebars": "^4.7.8"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "jest": "^29.7.0"
  }
}
